server:
  port: ${SERVER_PORT:8080}

spring:
  # 数据源配置（宝塔本机 MySQL，可用环境变量覆盖）
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:baseu}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:baseu}
    password: ${DB_PASSWORD:4XAEeNCPMb24A57A}
  
  # Redis配置
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

# JWT配置
jwt:
  secret: ${JWT_SECRET:baseu-prod-jwt-secret-change-me-8.211.144.162-aB3dEf9hIjKl2nOp}
  expiration: ${JWT_EXPIRATION:7200000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}

# 日志配置（生产只打控制台，避免宝塔因日志目录权限导致进程秒退）
logging:
  level:
    com.zero.baseu: INFO
    org.springframework.boot: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# 生产环境标识
app:
  environment: production
  name: BaseU Server (生产环境)
