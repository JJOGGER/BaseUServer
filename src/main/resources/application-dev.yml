server:
  port: 8080

spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/baseu?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: baseu
    password: 4XAEeNCPMb24A57A
  
  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
  

# JWT配置
jwt:
  secret: dev-secret-key-for-development-environment-change-in-production-aB3dEf9hIjKl2nOpQr5tUvWxYz

# 日志配置
logging:
  level:
    com.zero.baseu: DEBUG
    org.springframework.boot.autoconfigure: INFO
  file:
    name: logs/baseu-dev.log

# 开发环境标识
app:
  environment: development
  name: BaseU Server (开发环境)
