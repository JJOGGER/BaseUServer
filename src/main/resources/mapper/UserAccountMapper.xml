<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zero.baseu.mapper.UserAccountMapper">
    
    <select id="findByUserId" resultType="com.zero.baseu.entity.UserAccount">
        SELECT * FROM tb_user_account WHERE user_id = #{userId}
    </select>
    
    <update id="increaseBalance">
        UPDATE tb_user_account 
        SET balance = balance + #{amount},
            total_recharge = total_recharge + #{amount},
            version = version + 1
        WHERE user_id = #{userId} AND version = #{version}
    </update>
    
    <update id="decreaseBalance">
        UPDATE tb_user_account 
        SET balance = balance - #{amount},
            version = version + 1
        WHERE user_id = #{userId} 
          AND balance >= #{amount}
          AND version = #{version}
    </update>
    
</mapper>

