spring:
  datasource:
    url: jdbc:mysql://localhost:3306/baseu_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  
  data:
    redis:
      host: localhost
      port: 6379
  
  flyway:
    enabled: false

jwt:
  secret: test-secret-key-for-testing-only
  expiration: 7200000
  refresh-expiration: 604800000

logging:
  level:
    com.zero.baseu: DEBUG

